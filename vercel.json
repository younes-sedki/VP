{
  "buildCommand": "node scripts/remove-problematic-packages.js && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install && node scripts/remove-problematic-packages.js",
  "framework": "nextjs",
  "env": {
    "NEXT_PUBLIC_APP_URL": "https://www.sedkiy.dev",
    "NODE_ENV": "production"
  },
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
