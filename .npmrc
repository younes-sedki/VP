# Suppress peer dependency warnings for Storybook packages bundled with Next.js
# These are harmless warnings from Next.js's internal dependencies
# For npm
legacy-peer-deps=true
# For pnpm
strict-peer-dependencies=false

# Suppress unnecessary output
fund=false
audit=false
